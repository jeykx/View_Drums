{{ form_start(form) }}
<div>{{  form_row(form.imageFile) }}</div>

        {{ form_rest(form) }}
            <button class="btn btn-primary">{{ button|default('Enregistrer') }}</button>
    {{ form_end(form) }}

